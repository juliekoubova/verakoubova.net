{
  "name": "@verakoubova/monorepo",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "deploy": "lerna run --scope @verakoubova/web deploy"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "lerna": "^3.20.2",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.2",
    "typescript": "^3.8.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
---
layout: layouts/sidebar
tags: show
parentId: shows
eleventyComputed:
  pageId: "shows/{{ show }}"
---
{% set mp3 = (audio | url) if audio %}
<div data-controller="vr mt-4">
  <audio src="{{ mp3 }}" controls class="max-w-full">
    <a href="{{ mp3 }}">{{ audioSample }}</a>
  </audio>
</div>

<div class="applied mt-2">
  {{ content | safe }}
</div>

{% set gigs = shows[show] %}
{% include 'gigs.njk' %}
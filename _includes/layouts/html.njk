<!doctype html>
<html lang="{{ lang }}" class="{{ htmlClass }} xl:text-xl">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ titlePrefix }}{{ " - " if titlePrefix }}{{ title | lang }}</title>
    <link rel="stylesheet" href="{{ '/main.css' | url }}">
<link href="https://fonts.googleapis.com/css2?family=Fira+Sans+Condensed:ital,wght@0,300;0,500;1,300;1,500&family=Fira+Sans+Extra+Condensed:wght@300&display=swap" rel="stylesheet">


  </head>
  <body class="{{ bodyClass }} px-2">
    {{ content | safe }}
    <script src="{{ '/main.js' | url }}" async></script>
  </body>
</html>
<header class="flex flex-col md:flex-row md:justify-between pb-6 md:pb-8">
  <div>
    <nav class="vr-tight-sm">
      {% include 'nav.njk' %}
    </nav>
    {% if not bodyTitleInNav %}
    <h1 class="vr-tight-3xl">{{ bodyTitle | default(title) }}</h1>
    {% endif %}
  </div>
  <nav class="vr-tight-sm mt-2 md:mt-0 md:text-right">
    {% include 'lang-switcher.njk' %}
  </nav>
</header>

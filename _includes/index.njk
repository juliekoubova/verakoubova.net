---
layout: layouts/html
hasSidebar: true
---
{% extends 'sidebar.njk' %}

{% block before %}
<h1 class="sr-only">{{ site.name }}</h1>

<div class="scroll-snap-align-start">
  {% from 'macros.njk' import image %}
  <div
    data-controller="scroll-snap-mandatory"
    class="
      mx-auto max-w-xs flex flex-col flex-wrap px-2
      min-h-screen sm:max-w-none sm:flex-row-reverse sm:items-center"
    >
    <div
      class="vr-base flex-shrink-0 xl:pl-3 sm:flex-basis-1/2"
      >
      <div class="w-64 text-right">
        {% include 'lang-switcher.njk' %}
        {{ image(img, class = 'w-64') }}
      </div>
    </div>
    <nav
      class="
        vr-none-3xl text-right tracking-tight
        sm:flex-basis-1/2 sm:pr-2 md:pr-4 sm:vr-normal-4xl
      ">
      {{ content | safe }}
    </nav>
  </div>

  {% include 'scroll-enticer.njk' %}
</div>
<div class="scroll-snap-align-start min-h-screen" data-controller="adjust-vr">
{% endblock %}

{% block content %}
  {% for post in posts | reverse %}
  <article class="scroll-snap-align-start sidebar-section md:mb-4">
    <h2 class="in-sidebar vr-base">{{ post.title | lang }}</h2>
    <p class="vr-base para">{{ post | lang | safe }}</p>
  </article>
  {% endfor %}
{% endblock %}

{% block after %}
</div>
{% endblock %}
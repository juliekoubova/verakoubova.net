---
layout: applied
pageId: about
img:
  width: 420
  src: /img/vek.jpeg
  alt:
    cs: Věra Koubová s fotoaparátem
---
<div class="sidebar-section">

  <div class="in-sidebar mt-4 sm:float-right lg:flex lg:justify-end lg:float-none">
    <div class="mb-4 sm:mb-0 sm:mx-4 lg:ml-0 max-w-48">
      {% from 'macros.njk' import image %}
      {{ image(img) | safe }}
    </div>
  </div>

  <div>
    {{ content | safe }}

    <div class="vr-normal-base font-body text-right mt-8">
      <div><a href="mailto:{{ site.mail }}">{{ site.mail }}</a></div>
      {% if proofreading %}
      <div>{{ proofreading }}</div>
      {% endif %}
      <div>{{ audioDirection }} Martin Klusák</div>
    </div>
  </div>
</div>
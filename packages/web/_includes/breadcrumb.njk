<nav class="vr-normal-sm flex-auto"
  {% if breadcrumbLeaf %}
  data-controller="breadcrumb"
  {% endif %}
  >
  <ol class="flex flex-wrap content-end">
    {% for item in page | vekNav %}
    <li>
      <a href="{{ item.url }}">{{ item.label }}</a>
      <span aria-hidden="true">&rsaquo;&nbsp;</span>
    </li>
    {% endfor %}

    {% if breadcrumbLeaf %}
    <li hidden data-breadcrumb-target="parentItem">
      <a data-breadcrumb-target="parentAnchor">{{
        bodyTitle | default(title) | safe
      }}</a>
      <span aria-hidden="true" hidden data-breadcrumb-target="parentArrow">&rsaquo;&nbsp;</span>
    </li>
    <li data-breadcrumb-target="leafItem">
      <h2 class="inline" data-breadcrumb-target="leafHeading">{{
        bodyTitle | default(title) | safe
      }}</h2>
    </li>
    {% endif %}
  </ol>
</nav>
